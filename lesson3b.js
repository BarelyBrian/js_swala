// While loop keeps runnung as long as the condition is true. It exits the loop when the condition becomes false
let i=0;
 while(i<=10){//condition that we are testing
    console.log(i)
    // i++increment
}